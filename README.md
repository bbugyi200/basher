# basher

`basher` vendors shell scripts and the `bugyi.sh` shell library into projects.

This repository is under active development. The initial command set is
`vendor`, `lib`, `cat`, `path`, and `export`; run `basher --help` for details.

